module Stylesheet exposing (view)

import Html exposing (node)


view =
    let
        tag =
            "link"

        attrs =
            []

        children =
            []
    in
        node tag attrs children
